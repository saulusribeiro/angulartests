var minhavariavel = 'Minha Variavel Typescript';

function minhaFuncao(x,y){
    return x + y;
}
let num=2;
const PI=3.14;

var numeros = [1,2,3];
numeros.map(function (valor){return valor * 2;});

numeros.map( valor => valor * 2);

class Matematica {

    adicao(x,y){
        return x+y;
}}
